# Core service (unchanged from pre-rewrite).
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7

# Added for the NPZ-direct pipeline (Phase 1 clean-slate rewrite).
# numpy is already pulled in transitively by kimodo/torch, but pin it explicitly
# here so the `app/` modules can be installed and imported in a non-kimodo venv
# for testing.
numpy>=1.26
scipy>=1.11

# HTTP client for the retarget worker sidecar. fbxsdkpy itself is not
# installed here — it lives in the worker image (x86 emulated under qemu)
# because Autodesk's FBX SDK has no aarch64 build. See worker/server.py.
httpx>=0.27
